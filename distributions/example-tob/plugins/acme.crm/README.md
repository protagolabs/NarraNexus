# acme.crm

An example plugin bundled into the `acme.crm-agent` distribution by path (`narranexus-dist.json` -> `"acme.crm": {"path": "./plugins/acme.crm"}`). Bundled plugins boot in stage 1 with the builtins: they are the distribution's own code.
