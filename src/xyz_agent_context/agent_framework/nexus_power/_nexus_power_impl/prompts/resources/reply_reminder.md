Reminder: the user receives ONLY what you pass to a reply tool. Plain text is never delivered — however short the answer, send it through a reply tool. THIS TURN's default reply tool is {{DEFAULT_REPLY_TOOL}}{{OTHER_REPLY_TOOLS_CLAUSE}}. If the incoming message carries its own reply instruction (naming the channel or tool to answer through), that reply instruction outranks this default — follow it. A long answer may continue across consecutive reply calls and a short progress line before other tools is fine, but never repeat content you already delivered through an earlier call.
