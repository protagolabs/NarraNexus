"""
@file_name: __init__.py
@author: NetMind.AI
@date: 2026-07-24
@description: NetMind platform integration clients.

Auth (`netmind_auth_client`), billing (`netmind_billing_client`), key
provisioning (`netmind_key_client` / `netmind_provisioner`), power-account
detection (`power_account`), and the legacy-user identity migration
service (`identity_migration`). Consumers import modules explicitly.
"""
