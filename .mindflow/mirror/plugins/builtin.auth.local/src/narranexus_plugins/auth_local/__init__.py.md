---
code_file: plugins/builtin.auth.local/src/narranexus_plugins/auth_local/__init__.py
last_verified: 2026-09-04
stub: false
---

# builtin.auth.local — package

Package of the `builtin.auth.local` authProviders plugin (spec section 19.5): `provider.py` (the `kernel.auth` contribution) and `api.py` (facade). Distribution-only: selected by `narranexus-dist.json` `auth`, never installable at runtime. No import-time side effects.
