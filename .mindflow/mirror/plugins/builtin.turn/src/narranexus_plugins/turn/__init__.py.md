---
code_file: plugins/builtin.turn/src/narranexus_plugins/turn/__init__.py
last_verified: 2026-09-04
stub: false
---

# builtin.turn — package

Package of the default turn-pipeline plugin: the seven stage strategy modules (`ingress`, `recall`, `compose`, `assemble`, `act`, `commit`, `reflect`, moved from `platform/turn/stages/`), `profiles.py` (the profile contributions) and `stages.py` (the per-stage contribution tuples the manifest names). No import-time registration (batch 6b.2b).
