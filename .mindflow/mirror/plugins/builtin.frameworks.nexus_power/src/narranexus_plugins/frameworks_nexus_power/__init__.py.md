---
code_file: plugins/builtin.frameworks.nexus_power/src/narranexus_plugins/frameworks_nexus_power/__init__.py
last_verified: 2026-09-04
stub: false
---

# builtin.frameworks.nexus_power — package

Namespace package of the NexusPower framework plugin: `core/` is the framework runtime (formerly `agent_framework/nexus_power`), `adapter/` the `AgentLoopDriver` adapter (formerly `adapters/nexus`), `contribution.py` the framework contribution, `api.py` the facade. Import-time side effects: none.
