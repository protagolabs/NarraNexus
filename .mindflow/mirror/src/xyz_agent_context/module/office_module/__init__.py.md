---
code_file: src/xyz_agent_context/module/office_module/__init__.py
last_verified: 2026-07-13
stub: false
---

# __init__.py — OfficeModule package export

## Why it exists

Exports `OfficeModule` (see [[office_module]]) so the module registry
([[__init__]] under `module/`) can `from xyz_agent_context.module.office_module
import OfficeModule` and register it in `MODULE_MAP`.
