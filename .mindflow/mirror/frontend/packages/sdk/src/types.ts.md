---
code_file: frontend/packages/sdk/src/types.ts
last_verified: 2026-09-04
stub: false
---

# @narranexus/sdk — types.ts

Type-only re-exports of the app's own `HostAPI`/`Disposable` (platform/host) and registry definitions (platform/registries) — single source of truth; `tsconfig.build.json` emits them as .d.ts under `dist/types/`.
