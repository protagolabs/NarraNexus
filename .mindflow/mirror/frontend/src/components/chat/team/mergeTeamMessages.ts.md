---
code_file: frontend/src/components/chat/team/mergeTeamMessages.ts
last_verified: 2026-08-13
stub: false
---

# mergeTeamMessages — 把一次增量轮询折进已经在屏幕上的 transcript

## 为什么存在

房间每 3 秒重取全部 200 条消息。`since` 参数**端到端早就支持**，只是前端从来没传过。

**让全量重取正确的不是谨慎，是 `setMessages(all)`**：整体替换在构造上就是幂等的，
不可能重复、不可能陈旧。改成增量合并**必须把这个性质挣回来**，这个模块就是挣它的地方。

## 只追加去重，不做 reconciliation

`bus_messages` 在全仓**没有任何 UPDATE 路径**——消息写一次，永不修改。
试图 reconcile 编辑的合并，是在解决数据模型没有的问题，同时引入它不需要的复杂度。

**这个前提是承载性的，所以被单独钉住**（`tests/message_bus/test_team_message_segments.py`
里有一条测试扫描全仓，断言没有 `update("bus_messages")`）。哪天消息变成可变的，
那条测试会先红，而这个文件是必须跟着改的那个。

## 三个细节

- **去重是必须的**：`since` 在某些时钟上是闭区间，轮询窗口自我重叠，最新那条**一定会再回来**。
  不去重的话房间每 3 秒长出一条重复。
- **无变化时返回原数组引用**：React 按引用重渲染。每 3 秒返回新数组等于每分钟白重渲 20 次整个
  transcript，**并把读者已经展开的折叠重置掉**。
- **按时间排序，不按到达顺序**：两个 agent 同时回话可能跨轮询边界乱序到达。

## 游标是水位线，不是最后一条

取尾元素会在乱序插入后**让游标倒退**（重复拉取），或者更糟——**前进越过从未见过的消息**。
一条无法解析的时间戳被跳过，否则它会把游标推成 NaN，之后每次轮询都卡住。
