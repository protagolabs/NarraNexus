---
code_file: frontend/src/components/artifacts/renderers/__tests__/BrowserStreamPanel.regression.test.tsx
last_verified: 2026-09-24
stub: false
---

2026-09-24：人工新建标签后地址栏聚焦；网址规范化后向具体 page_id 发送导航，等待
回执期间禁用重复操作，错误可重试，不支持协议不发请求，旁观禁用导航并隐藏关闭。
交还后清除尚未提交的输入，恢复真实网址。
标签整行可见性回归覆盖接管出现关闭按钮和标签容器尺寸变化，分别模拟 tablist 与
canvas 的 ResizeObserver，不能让标题滚动代替包含关闭按钮的父容器滚动。

## 2026-09-23 多页面支持

新增多个标题/网址与标签选择、恢复跟随、迟到旧页帧和异步解码隔离。hello 早到、
运行时查询尚未返回时，晚挂载 canvas 仍须发 resize。所有消息替身携带真实 page_id。

# 浏览器连接与输入回归

集中保护跨异步边界的行为：首帧鉴权、断线重连、切换智能体后拒收旧回调、图片解码顺序、
多窗口控制权，以及按实际面板尺寸请求远程 viewport。连接替身允许确定性制造真实网络中
难稳定复现的竞态。输入测试覆盖中文组合输入、粘贴、触摸滚动和移出画面的鼠标释放，
避免只验证普通 ASCII 点击而遗漏接管登录时的核心路径。
