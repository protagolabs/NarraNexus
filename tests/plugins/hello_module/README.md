# Hello Module

A plugin that contributes an L4 module (`AcmeNotesModule`): instructions, one MCP tool (`note_add`), decision metadata for the instance-decision prompt, a per-agent instance and the `ext_acme_hello_module__notes` table. The batch 5 exit criterion: a non-builtin module installs as a plugin and takes part in a turn.
