# Narramessenger (`builtin.channels.narramessenger`)

Builtin module NarramessengerModule (IM channel: trigger + module + tools).

A NarraNexus builtin plugin, packaged as `narranexus-plugin-channels-narramessenger` (plugin platform batch 6b). Code: `src/narranexus_plugins/narramessenger_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
