# Awareness (`builtin.awareness`)

Builtin module AwarenessModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-awareness` (plugin platform batch 6b). Code: `src/narranexus_plugins/awareness_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
