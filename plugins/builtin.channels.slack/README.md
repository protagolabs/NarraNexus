# Slack (`builtin.channels.slack`)

Builtin module SlackModule (IM channel: trigger + module + tools).

A NarraNexus builtin plugin, packaged as `narranexus-plugin-channels-slack` (plugin platform batch 6b). Code: `src/narranexus_plugins/slack_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
