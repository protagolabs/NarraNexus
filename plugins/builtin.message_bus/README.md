# MessageBus (`builtin.message_bus`)

Builtin module MessageBusModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-message-bus` (plugin platform batch 6b). Code: `src/narranexus_plugins/message_bus_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
