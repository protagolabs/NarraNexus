# Helper LLM clients (`builtin.llm_clients`)

anthropic / openai / cli protocol clients for the helper-LLM slot.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-llm-clients` (plugin platform batch 6b). Code: `src/narranexus_plugins/llm_clients/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
