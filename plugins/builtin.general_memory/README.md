# GeneralMemory (`builtin.general_memory`)

Builtin module GeneralMemoryModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-general-memory` (plugin platform batch 6b). Code: `src/narranexus_plugins/general_memory_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
