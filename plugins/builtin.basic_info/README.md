# BasicInfo (`builtin.basic_info`)

Builtin module BasicInfoModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-basic-info` (plugin platform batch 6b). Code: `src/narranexus_plugins/basic_info_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
