# Job (`builtin.job`)

Builtin module JobModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-job` (plugin platform batch 6b). Code: `src/narranexus_plugins/job_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
