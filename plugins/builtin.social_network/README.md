# SocialNetwork (`builtin.social_network`)

Builtin module SocialNetworkModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-social-network` (plugin platform batch 6b). Code: `src/narranexus_plugins/social_network_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
