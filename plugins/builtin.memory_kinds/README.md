# Memory kinds (`builtin.memory_kinds`)

event / bus / narrative / entity / job / observation memory kinds.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-memory-kinds` (plugin platform batch 6b). Code: `src/narranexus_plugins/memory_kinds/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
