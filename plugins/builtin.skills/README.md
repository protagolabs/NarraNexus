# Skill (`builtin.skills`)

Builtin module SkillModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-skills` (plugin platform batch 6b). Code: `src/narranexus_plugins/skill_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
