# CommonTools (`builtin.common_tools`)

Builtin module CommonToolsModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-common-tools` (plugin platform batch 6b). Code: `src/narranexus_plugins/common_tools_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
