# HomeAssistant (`builtin.home_assistant`)

Builtin module HomeAssistantModule.

A NarraNexus builtin plugin, packaged as `narranexus-plugin-home-assistant` (plugin platform batch 6b). Code: `src/narranexus_plugins/home_assistant_module/`; manifest: `narranexus-plugin.json` (mirrored by `narranexus.kernel.plugins.builtins` until distributions generate it). Depends on the engine (`narranexus`) only.
