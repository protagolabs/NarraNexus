# narranexus-sdk

NarraNexus plugin SDK for Python: the PluginTestHost and testing helpers a plugin runs its tests against.

Part of the `narranexus` namespace package (`import narranexus.sdk`); shipped as its own wheel so a plugin can depend on the contracts alone (plugin platform batch 6d, spec section 19.7). Versioned in lockstep with the engine.
